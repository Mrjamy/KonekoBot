{
    "connections": {
        "default": {
            "engine": "tortoise.backends.asyncpg",
            "credentials": {
                "host": "localhost",
                "port": "5432",
                "user": "myusername",
                "password": "mypassword",
                "database": "koneko"
            }
        }
    },
    "apps": {
        "models": {
            "models": [
                "src.helpers.database.models.level"
            ],
            "default_connection": "default"
        }
    }
}
